# <模块名> 功能说明

<!--
  功能说明模板（design-flow 阶段 0 产物骨架）。使用方法：
  1. 复制为 doc/md/<模块名>功能说明.md，逐处替换 <占位符>；
  2. 注释行（<!‐‐ ‐‐>）是填写指南，定稿后可删；
  3. 机检脚本 req_check.py 按本模板结构检查，检查点在各节注释里标明；
     保持结构不变即可一次过检。
-->

<一句话定位：这个模块是什么，对上游/下游各提供什么接口，时钟域概况。>

> design-flow：阶段 0（功能概述 + 需求清单）。状态：<draft / G1 审查中 /
> 已过 G1（commit <id>）>。阶段 1 架构设计单独成文：
> `doc/md/<模块名>架构设计.md`。

## 功能概述

<!-- 回答"这个模块能做什么、要做什么"：叙事描述，不含实现细节。
     建议覆盖：主功能（端到端行为）、特殊事务、管理与观测、"不做的事"。 -->

<模块名> 是 <……>，主功能是 <……>。它能做、要做的事：

1. **<能力1，加粗短语>**：<一到三句行为描述>；
2. **<能力2>**：<……>；
3. **<……>**：<……>。

<模块名> 不做的事：<明确排除项，防上游/软件越界假设>。

## 需求清单（REQ）

来源：<系统需求（项目输入 日期）/ 协议标准（编号）/ 设计决策>。
优先级：P0 基本功能 / P1 重要场景 / P2 边角。
组织方式：树形功能分解——每个大类一句"功能全集定义"，类内小类在行为上
互相独立、合起来等于大类全集（审查完备性时逐节点问"小类是否把大类
说全"）；每条 REQ 恰好归属一个叶子（别处需要时用文字引用，不重复挂）。
REQ 编号全局连续、不随结构重排；正文条款号在阶段 1 正文完成后补登回指；
`doc/yaml/spec.yaml` 在阶段 2 由本文件正向提取。

<!--
  机检（req_check.py）：
  ① 每条 REQ 行四要素齐全：id 符合 REQ-\d{3}、需求/来源/优先级非空
    （优先级限 P0/P1/P2）；
  ② 需求文字禁"等/若干/尽量"类模糊词（白名单：等价/不等/等待/停等）；
  ③ 树结构：每个 ### 大类有"全集定义"句；每个 #### 叶子有导读行；
     REQ 行只出现在 #### 叶子表格中；空类显式写"无"；#### N.M 编号
     与 ### N. 一致；
  ④ REQ-id 全局唯一；全文引用的 REQ-id 必须存在。

  L1 大类参考骨架（按设计形态裁剪）：接口与形态 / 写通路 / 读通路 /
  读写共享语义 / 管理面 / 观测面 / 性能与范围。通路类小类建议按
  "受理→转换→执行→响应"流水阶段切。
-->

### 1. <大类名，如：接口与形态>

本类功能全集：<一句话说全这类覆盖什么边界>。

#### 1.1 <小类名>

<导读：本叶子回答什么>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-001 | <……> | <……> | P0 |

#### 1.2 <小类名>

<导读>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-002 | <……> | <……> | P0 |

### 2. <大类名，如：写通路（受理 → 转换 → 执行 → 响应）>

本类功能全集：<……>。

#### 2.1 <小类名，如：受理>

<导读>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-003 | <……> | <……> | P0 |

#### 2.2 <小类名>

<导读>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-004 | <……> | <……> | P0 |

### 3. <大类名，如：读通路>

本类功能全集：<……>。

#### 3.1 <小类名>

<导读>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-005 | <……> | <……> | P0 |

### 4. <大类名，如：读写共享语义>

本类功能全集：<……>。

#### 4.1 <小类名>

<导读>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-006 | <……> | <……> | P0 |

### 5. <大类名，如：管理面>

本类功能全集：<……>。

#### 5.1 <小类名>

<导读>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-007 | <……> | <……> | P1 |

### 6. <大类名，如：观测面>

本类功能全集：<……>。无则显式写：无。

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-008 | <……> | <……> | P1 |

### 7. <大类名，如：性能与范围>

本类功能全集：<……>。

#### 7.1 <小类名>

<导读>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-009 | <……> | <……> | P2 |

### 假设清单（阶段 0 登记）

<!-- 机检：每条含"确认（日期/确认人）"或"转 doc/md 功能提问文档（QUE-id）"。 -->

1. <假设内容与默认值>。处置：<G1 人审确认 / 转功能提问文档 QUE-001>；
2. <……>。

当前无未关闭的功能提问；后续产生提问时按 design-flow 阶段 0 约定登记
doc/md 功能提问文档（md 形式，不做 yaml 追踪）。
