# <模块名> 功能说明

<!--
  功能说明模板（design-flow 阶段 0 产物骨架）。使用方法：
  1. 复制为 doc/md/<模块名>功能说明.md，逐处替换 <占位符>；
  2. 注释行（<!‐‐ ‐‐>）是填写指南，定稿后可删；
  3. 机检脚本 req_check.py 按本模板结构检查，检查点在各节注释里标明；
     保持结构不变即可一次过检。
-->

<一句话定位：这个模块是什么，对上游/下游各提供什么接口，时钟域概况。>

> design-flow：阶段 0（功能概述 + 需求清单）。状态：<draft / G1 审查中 /
> 已过 G1（commit <id>）>。阶段 1 架构设计单独成文：
> `doc/md/<模块名>架构设计.md`。

## 功能概述

<!-- 回答"这个模块能做什么、要做什么"：叙事描述，不含实现细节。
     建议覆盖：主功能（端到端行为）、特殊事务、管理与观测、"不做的事"。 -->

<模块名> 是 <……>，主功能是 <……>。它能做、要做的事：

1. **<能力1，加粗短语>**：<一到三句行为描述>；
2. **<能力2>**：<……>；
3. **<……>**：<……>。

<模块名> 不做的事：<明确排除项，防上游/软件越界假设>。

## 需求清单（REQ）

来源：<系统需求（项目输入 日期）/ 协议标准（编号）/ 设计决策>。
优先级：P0 基本功能 / P1 重要场景 / P2 边角。REQ 按功能分类，编号不随
分类重排（保证审核期引用稳定）；正文条款号在阶段 1 正文完成后补登回指；
`doc/yaml/spec.yaml` 在阶段 2 由本文件正向提取。

<!--
  机检（req_check.py）：
  ① 每条 REQ 行四要素齐全：id 符合 REQ-\d{3}、需求/来源/优先级非空
     （优先级限 P0/P1/P2）；
  ② 需求文字禁"等/若干/尽量"类模糊词（白名单：等价/不等/等待/停等）；
  ③ 每个分类小节表格前有一句导读；某类无内容时显式写"无"，不许删类；
  ④ REQ-id 全局唯一；假设清单与正文引用的 REQ-id 必须存在。
-->

### 1. 接口与形态

<导读：本模块对外长什么样——对外接口、时钟复位、地址与参数空间。>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-001 | <……> | <……> | P0 |

### 2. 主功能与数据变换

<导读：核心处理/协议转换/格式或位宽变换——把什么变成什么。无数据变换的
设计（纯控制类）显式写"无"。>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-002 | <……> | <……> | P0 |

### 3. 顺序与响应语义

<导读：多个事务之间的先后规则；响应何时回、回什么；流控约定。>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-003 | <……> | <……> | P0 |

### 4. 功能类型与特殊场景

<导读：事务类型/工作模式/边角场景（窄传输、部分写、zero-mask 这类）。>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-004 | <……> | <……> | P0 |

### 5. 管理面与观测

<导读：配置/状态/统计/错误上报。无管理面的设计显式写"无"。>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-005 | <……> | <……> | P1 |

### 6. 性能目标与缓冲原则

<导读：跑多快、buffer 为什么而存在（正确性是否依赖深度的声明）。>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-006 | <……> | <……> | P1 |

### 7. 上游约束与范围排除

<导读：上游必须遵守的约定（本模块不做硬件检查的清单），以及明确不做什么。>

| REQ | 需求 | 来源 | 优先级 |
|---|---|---|---|
| REQ-007 | <……> | <……> | P0 |

### 假设清单（阶段 0 登记）

<!-- 机检：每条含"确认（日期/确认人）"或"转 doc/md 功能提问文档（QUE-id）"。 -->

1. <假设内容与默认值>。处置：<G1 人审确认 / 转功能提问文档 QUE-001>；
2. <……>。

当前无未关闭的功能提问；后续产生提问时按 design-flow 阶段 0 约定登记
doc/md 功能提问文档（md 形式，不做 yaml 追踪）。
