# <模块名> 架构设计

<!--
  架构设计模板（design-flow 阶段 1 产物骨架，方法按 design-flow §6）。
  1. 复制为 doc/md/<模块名>架构设计.md，逐处替换 <占位符>；
  2. 注释行是填写指南，定稿后可删；
  3. 机检脚本 arch_check.py 按本模板结构检查，核心：§4 十二项检查单
     一行不缺、每行三态齐全。保持结构即可一次过检。
  顺序要求：先 §1 性能模型（数字）后 §5 模块划分（图纸）。
-->

> design-flow 阶段 1 产物。需求基线：`doc/md/<模块名>功能说明.md`
> （REQ-001~REQ-0xx，已过 G1）。状态：<draft / G2 审查中 / 已过 G2
> （commit <id>）>。

## 1. 需求与性能指标（定量基线）

| 项 | 数值 | 来源 |
|---|---|---|
| <上游接口速率/位宽> | <定量> | <REQ-id> |
| <下游接口速率/位宽> | <定量> | <REQ-id> |
| 速率比与瓶颈 | <谁快谁慢、瓶颈在哪> | <REQ-id> |
| 带宽目标 | <稳态指标> | <REQ-id> |
| 事务粒度 | <……> | <REQ-id> |
| 时钟 | <时钟域划分> | <REQ-id> |

假设清单：见功能说明需求清单章末（逐条确认状态）。

## 2. 候选方案对比

### 方案 A：<名称>【选定 / 排除】

- 结构：<数据流经过什么、做什么>；
- 满性能条件推导：<数字：每级"数据拍数/事务拍数" ≥ 目标速率比；命令与
  返回两个方向分别推导>；
- 优点：<……>；缺点：<……>。

### 方案 B：<名称>【选定 / 排除】

<同上；排除必须给理由>

选定方案 <X> 后的关键取舍：<删掉/简化了什么，为什么成立>。

## 3. 数据流

### 3.1 写

1. <逐步：哪个模块做什么变换>；

### 3.2 读

1. <……>。

## 4. 定量分析

按 design-flow §6 检查单逐项给结论（三态：结论 / 不适用+原因 / 待确认）。

<!--
  机检（arch_check.py）：
  ① 下表 12 项一行不缺（缺行 = 静默跳过，不过门）；
  ② 每行结论格非空；含"不适用"必须带原因；含"待确认"必须与 §7 开放
     问题对得上。
-->

| 项 | 结论 |
|---|---|
| 事务间气泡 | <相邻事务有无空闲拍；命令与返回两个方向分别推导> |
| 握手体制 | <有无 ready；无 ready 必须 credit 制> |
| 速率差缓冲 | <每处生产者>消费者接口的 buffer 深度依据；反压堵谁> |
| 跨时钟信用 | <多时钟域时：异步 FIFO 深度 ≥ RTT×消费速率；单时钟写"不适用+原因"> |
| 保序/重排 | <缓冲量与头阻塞代价> |
| 计数器健康 | <会回绕吗；能否用 FIFO 自身状态替代计数器> |
| 串行化点 | <"一次一个事务"环节：服务时间×到达率 < 1 吗> |
| 状态机边界 | <带宽敏感通路不用逐事务 FSM；FSM 仅限真多阶段串行序列> |
| 仲裁位置 | <仲裁点紧贴被共享资源；无争用写"不适用+原因"> |
| 慢事件摊薄 | <周期/偶发慢操作期间其余通路能否继续；占比多少> |
| 反压路径 | <每级满时反压谁；会否经共享通道堵死无关流量> |
| 流水级数 | <每级逻辑深度可收敛吗；打拍位置切断跨模块长路径> |

buffer 初值（例化参数，正确性不依赖深度）：

| buffer | 深度 | 依据 |
|---|---|---|
| <名称> | <深度> | <定量依据：覆盖什么场景> |

**<关键 buffer 取舍决策记录（日期+确认）>**：<定量推导与结论>。

## 5. 模块划分与接口定义

<结构简图（文字箭头，按通路分开画：命令下行/读返回/写响应）>

| 模块 | 时钟 | 职责 | 例化参数 |
|---|---|---|---|
| <……> | <……> | <……> | <……> |

内部接口（valid/ready）：

1. <接口名>：<信号集合、方向、语义、反压约定>；

顶层端口：<按组列：上游/下游/管理/时钟复位>

CSR 地址映射（初版，阶段 2 由 spec.yaml 定稿）：

| 区域 | 地址 | 名称 | 属性 | 功能 |
|---|---|---|---|---|
| <……> | <……> | <……> | <RW/RO/W1T…> | <reset 值与语义> |

## 6. 资源/面积粗估

<存储逐项（位宽×深度）+ 数据通路寄存器 + 合计；实现方式判断（寄存器/SRAM）>

## 7. 风险与待确认项

外部依赖（<对端/厂商>行为，以 <datasheet/厂商确认> 为准；若与设计假设
矛盾，走 spec 升版流程）：

1. <……>；

开放问题：

- <……>。
