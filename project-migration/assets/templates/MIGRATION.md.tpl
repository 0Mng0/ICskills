# 工程迁移清单（MIGRATION.md）

> 由 project-migration 技能维护：旧工程导出时填写，新工程导入时先读并按"差异与决策"节回写。

## 迁移信息
- 旧工程：<路径/仓库>
- 新工程：<路径/仓库，导出时可留空>
- 导出时间：<日期>
- 迁移理由：<为什么迁：上下文耗尽/重构/换仓库…>

## 迁移范围
- 要迁：<概述，详见 migration-map.json>
- 不迁：build 产物、依赖目录、IDE 缓存（及本工程特有的：…）
- 禁止迁：密钥与凭证（新工程按下方变量名重建）

## 环境与依赖
- 工具链/版本：<语言、编译器、仿真器…>
- 依赖清单：<requirements.txt / package.json / 其他>
- 需要重建的环境变量（只列名字）：<FOO, BAR…>
- 外部资源：<EDA 工具、许可证、服务器…>

## 关键决策
- 决策：…（理由：…，见文件…）

## 遗留问题
- 已知 bug / 未完成项 / flaky 测试

## 差异预警（新工程已知的不同点）
- <目录结构 / 版本 / 命名约定…>

## 验证标准（新工程里判断迁移成功的检查）
- 构建：<命令> → 预期：…
- 测试：<命令> → 预期：…
- 冒烟：<步骤> → 预期：…

## 差异与决策（import 时回写）
- <路径/依赖/版本差异及处理方式>

## 旧工程最终快照
- `git status`：<摘要>
- `git log --oneline -10`：
```
<粘贴>
```
